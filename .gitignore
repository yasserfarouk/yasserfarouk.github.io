# Jekyll build output (deployed separately into docs/, which IS tracked)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
.bundle/
vendor/bundle/
Gemfile.lock

# Node (if any tooling under docs/ needs it)
node_modules/

# OS / editor cruft
.DS_Store
*.swp
*~
